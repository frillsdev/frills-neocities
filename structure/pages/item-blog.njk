{% extends "structure/templates/page-3col.njk" %}

{%- block left -%}
    {% if pics %}
    {%- for pic in pics %}
    <img src="{{ pic.src }}" alt="{{ pic.alt }}" class="mw-90">
    {%- endfor %}
    {% endif %}
{%- endblock -%}

{%- block main -%}
    {% import "structure/components/components.njk" as macro with context %}

    <article class="full-width">
        <header class="mb-2">
            <h1>{{title}}</h1>
            {% if tags.includes("weeknotes") %}
            {% else %}
            <small>Written on <time datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate }}</time></small>
        </header>
        {% endif %}
        <div class="reading-width{% if tags.includes("weeknotes") %} h-sm{% endif %}">
            {{ content | safe }}
        </div>
    </article>
{%- endblock -%}

{%- block right -%}
    <ul class="unstyled">
        {% if tags %}
        <li class="mb-1">
            {%- for tag in tags | filterTagList %}
            <span class="tag">#{{ tag }}</span>{%- if not loop.last %} {% endif %}
            {%- endfor %}
        </li>
        {% endif %}
        {% if recipe.time %}
        <li>⏰ Recipe time: {{recipe.time}}</li>
        {% endif %}
        {% if recipe.cooking %}
        <li>🔥 Cooking time:  {{recipe.cooking}}</li>
        {% endif %}
        {% if recipe.tools %}
        <li>🥣 Tools: {{recipe.tools}}</li>
        {% endif %}
    </ul>
{%- endblock -%}

{% block scripts %}
{% endblock %}